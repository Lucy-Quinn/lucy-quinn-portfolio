const cvVariants = {
    hover: {
        scale: 1.1,
        transition: { duration: 1 },
    }
}

const deployVariants = {
    hover: {
        scale: 1.2,
        transition: { duration: 1 },
    }
}

const githubVariants = {
    hover: {
        scale: 1.2,
        transition: { duration: 1 },
    }
}

export {
    cvVariants,
    deployVariants,
    githubVariants
}